
server:
  port: 8080
  managementPort: 8081

api:
  key: ${GOV_NOTIFY_API_KEY_TEST}

template:
  id: ${GOV_NOTIFY_TEMPLATE_ID}

invite:
  validityInSeconds: 86400

accessToken:
  validityInSeconds: 86400
