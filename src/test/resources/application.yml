
server:
  port: 8080
  managementPort: 8081

govNotify:
  key: ${GOV_NOTIFY_API_KEY_TEST:key}
  template: ${GOV_NOTIFY_TEMPLATE_ID:template}

invite:
  validityInSeconds: 86400

accessToken:
  validityInSeconds: 86400
